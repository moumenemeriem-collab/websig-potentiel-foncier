services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: websig
      POSTGRES_USER: websig
      POSTGRES_PASSWORD: websig_secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
